{
  "name": "Intelligent RAG",
  "description": "A function that uses the intelligent-rag-server for enhanced RAG capabilities",
  "parameters": {
    "type": "object",
    "properties": {
      "projectId": {
        "type": "string",
        "description": "The project ID to query against"
      },
      "thinkingDepth": {
        "type": "integer",
        "description": "Depth of thinking (1-4). Higher values use more sophisticated processing",
        "default": 2,
        "minimum": 1,
        "maximum": 4
      }
    },
    "required": ["projectId"]
  },
  "api": {
    "url": "http://localhost:3000/api/query",
    "method": "POST",
    "headers": {
      "Content-Type": "application/json"
    }
  },
  "examples": [
    {
      "name": "Basic Query",
      "parameters": {
        "projectId": "default",
        "thinkingDepth": 2
      },
      "input": "What is the main theme of chapter 1?"
    },
    {
      "name": "Deep Analysis",
      "parameters": {
        "projectId": "default",
        "thinkingDepth": 4
      },
      "input": "Compare and contrast the characters' motivations throughout the story."
    }
  ]
}